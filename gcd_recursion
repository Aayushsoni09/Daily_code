def gcd(a,b):
  if int(a)!=a or int(b)!=b:
    return "a and b should be integers"
  else:
    if a<0:
      a=a*-1
    if b<0:
      b=b*-1
    if b==0:
      return a
    else:
      return gcd(b,a%b)
print(gcd(6,18))      
